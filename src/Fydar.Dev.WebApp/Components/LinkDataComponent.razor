@if (Model != null && Model.Count > 0)
{
    <script type="application/ld+json">
        @(new Microsoft.AspNetCore.Components.MarkupString(Model.ToJson()))
    </script>
}

@code {
    [Parameter, EditorRequired]
    public LinkData Model { get; set; } = null!;
}
